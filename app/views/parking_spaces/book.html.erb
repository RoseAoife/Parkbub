<h1>Checkout</h1>
<%- form_tag charges_path do %>
<article>
  <% if flash[:error].present? %>
    <div id='error_explanation'>
      <p><%= flash[:error] %></p>
    </div>
  <% end %>
  <label class='amount'>
    <span>Amount: €<%= @order.price %></span>  
  </label>
</article>

<script scr="https://checkout.stripe.come/checkout.js" class="stripe-button" data-key='<%= Rails.configuration.stripe[:publishable_key] %>' data-description="Space's price for allotted time" data-amount='<%= @order.price %>' data-locale='auto'></script>
<% end %>